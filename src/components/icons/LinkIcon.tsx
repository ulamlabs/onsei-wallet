import { Colors } from "@/styles";
import { LinkCircle } from "iconsax-react-native";

export default function LinkIcon() {
  return (
    <LinkCircle variant="Bold" size={18} color={Colors.markerBackground} />
  );
}
