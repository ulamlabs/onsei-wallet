import { Colors } from "@/styles";
import { InfoCircle } from "iconsax-react-native";

export default function NoBackupIcon() {
  return <InfoCircle variant="Bold" size={18} color={Colors.warning} />;
}
