import { NavigatorParamsList } from "@/types";
import { NativeStackScreenProps } from "@react-navigation/native-stack";

import React from "react";

type DashboardProps = NativeStackScreenProps<NavigatorParamsList, "Bridge">;

export default function BridgeAggregator({ navigation }: DashboardProps) {
  return <></>;
}
